<form method="post" action="{{ path('rec_personne_perdue_delete', {'id': rec_personne_perdue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rec_personne_perdue.id) }}">
    <button class="btn">Delete</button>
</form>
