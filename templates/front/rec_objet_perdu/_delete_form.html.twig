<form method="post" action="{{ path('rec_objet_perdu_delete', {'id': rec_objet_perdu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rec_objet_perdu.id) }}">
    <button class="btn">Delete</button>
</form>
